language: ruby
sudo: false
cache: bundler
before_install: gem install bundler
bundler_args: --without yard guard benchmarks
script: "rake spec"

rvm:
  - 2.0.0
  - 2.1.0
  - 2.1.1
  - 2.1.2
  - 2.1.5
  - 2.2.4
  - 2.3.0
  - jruby-19mode
  - ruby-head

matrix:
  allow_failures:
    - rvm: ruby-head